#!/usr/bin/make -f

include /usr/share/cdbs/1/rules/debhelper.mk
include /usr/share/cdbs/1/class/autotools.mk

# To look for shlibs and symbols file in locally compiled libgts-snapshot debian package
# do something like:
# % export DEB_DH_SHLIBDEPS_ARGS_ALL="-- -S$HOME/local/src/gts-mainline/debian/libgts-snapshot -S$HOME/local/src/gerris-devel/debian/gerris-snapshot"
